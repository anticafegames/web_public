import styled from 'styled-components'

import Modal from '../../modal/styled'

export default styled(Modal)`

.modal {
    height: 270px;
    max-width: 500px;
}

.create-room-form {
    .custom-checkbox {
        margin: 10px 0 15px;
    }
}

`